const express = require('express')
const router = express.Router();

router.post('/course/:courseId/review/upload', (req,res)=>{
    const courseId = req.params.courseId;
    const review = req.body.review;

    try {
        
    } catch (error) {
        
    }
})